@model KEC.Curation.UI.Models.RoleViewModel

@{
    ViewBag.Title = "Manage Roles";
    Layout = "~/Views/Shared/_LayoutPassword.cshtml";
}
<h3 style="color:red;">Are you sure you want to delete this?</h3>
<div>
    <h4>Delete Role</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

    </dl>

    @using (Html.BeginForm("DeleteConfirmed", "Role", new { id = Model.Id, }))
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-danger" />
            &nbsp; &nbsp; &nbsp;<a href="@Url.Action("Index", "Role")" class="btn btn-primary" style="background-color:#00b95f;"> <i class="fa fa-arrow-circle-left"></i> Back to List </a>

        </div>
    }
</div>
