@using System.Collections.Generic;
@using KEC.Curation.UI.Models;
@model  List<ActiveDirectoryUser>
    @{
        ViewBag.Title = "Users";
        Layout = "~/Views/Shared/_LayoutPrincipalCurator.cshtml";
    }

    <h2>Users</h2>
    @foreach(var user in Model)
    {
        <p>@user.DisplayName</p>
    }

